[project]
name = "gauge"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mujoco>=3.2.0",
    "casadi>=3.6.5",
    "numpy>=1.26",
    "opencv-python>=4.10",
    "pyyaml>=6.0",
    "defusedxml>=0.7.1",
]

[dependency-groups]
dev = ["pytest>=8.0"]
